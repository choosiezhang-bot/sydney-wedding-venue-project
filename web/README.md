# Web dashboard

## Run locally

From repo root:

```bash
python3 -m http.server 8000
```

Then open:

- http://localhost:8000/web/

> Important: must use a local server (http://). Opening `web/index.html` via `file://` will block `fetch()`.

## Data source

- `data/venue_longlist.csv`

